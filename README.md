# Inventory Management (Skillwise)
Full-stack sample project (React frontend, Node/Express backend, SQLite database).

## Setup

### Backend
cd backend
npm install
cp .env.example .env
npm run dev

### Frontend
cd frontend
npm install
npm start
